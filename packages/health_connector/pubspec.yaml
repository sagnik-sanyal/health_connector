name: health_connector
resolution: workspace
description: A Flutter plugin to access health data from Health Connect (Android) and HealthKit (iOS)
version: 1.4.0
homepage: https://github.com/fam-tung-lam/health_connector
repository: https://github.com/fam-tung-lam/health_connector/tree/main/packages/health_connector

environment:
  sdk: ^3.9.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  health_connector_core: ^1.4.0
  health_connector_hc_android: ^1.4.0
  health_connector_hk_ios: ^1.4.0
  health_connector_logger: ^1.0.1
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  health_connector_lint: ^1.1.0

platforms:
  android:
  ios:
