name: health_connector_toolbox
resolution: workspace
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  health_connector: ^3.1.0
  intl: ^0.19.0
  provider: ^6.1.5+1
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  health_connector_lint: ^1.2.0

flutter:
  uses-material-design: true

