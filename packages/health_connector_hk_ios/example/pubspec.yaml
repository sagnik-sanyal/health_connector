name: health_connector_hk_ios_example
resolution: workspace
description: "Demonstrates how to use the health_connector_hk_ios plugin."
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  health_connector_core: ^1.2.1
  health_connector_hk_ios: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  health_connector_lint: ^1.1.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
