name: health_connector_toolbox
resolution: workspace
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  health_connector:
    path: ../../packages/health_connector
  health_connector_core:
    path: ../../packages/health_connector_core
  intl: ^0.19.0
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  health_connector_lint:
    path: ../../packages/health_connector_lint

flutter:
  uses-material-design: true

license: Apache-2.0
